<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="insertBoard">
        insert into MatchingBoard(category, teamname, writer, content, startdate, starttime,
                                  numpeople)
        values
        (#{category}, #{teamname}, #{writer}, #{content}, #{startdate}, #{starttime},
         #{numpeople})
    </insert>
    <update id="updateBoard">
        update MatchingBoard
        set = category=#{category}, teamname=#{teamname}, writer=#{writer}, content=#{content},
            startdate=#{startdate}, starttime=#{starttime}, numpeople=#{numpeople}
        where id=#{id}
    </update>
    <delete id="deleteBoard">
        delete from MatchingBoard where id=#{id}
    </delete>
    <select id="getBoard" resultType="board">
        select * from MatchingBoard where id=#{id}
    </select>
    <select id="getBoardList" resultType="board">
        select * from MatchingBoard order by id desc
    </select>
</mapper>